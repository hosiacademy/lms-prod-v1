# Create or overwrite the SSH config file
echo -e "Host entai\n    HostName 154.66.211.3\n    User tk\n    Port 2222\n    IdentityFile ~/.ssh/id_rsa" > ~/.ssh/config
# Set correct permissions (important for security)
chmod 600 ~/.ssh/config
# (Optional) Make sure the .ssh folder has correct permissions too
chmod 700 ~/.ssh
cls
mkdir -p ~/.ssh
echo -e "Host entai\n    HostName 154.66.211.3\n    User tk\n    Port 2222\n    IdentityFile ~/.ssh/id_rsa" > ~/.ssh/config
chmod 700 ~/.ssh
chmod 600 ~/.ssh/config
mkdir -p ~/.ssh
echo -e "Host entai\n    HostName 154.66.211.3\n    User tk\n    Port 2222\n    IdentityFile ~/.ssh/id_rsa" > ~/.ssh/config
chmod 700 ~/.ssh
chmod 600 ~/.ssh/config
ssh entai
sudo apt update && sudo apt update -y
sudo apt install -y git unzip xz-utils zip libglu1-mesa clang cmake ninja-build pkg-config libgtk-3 dev
sudo apt update
sudo apt install -y git unzip xz-utils zip libglu1-mesa clang cmake ninja-build pkg-config libgtk-3-dev
cd ~git clone https://github.com/flutter/flutter.git -b stable flutter
git clone://github.com/flutter/flutter.git -b stable flutter
cd ~ git clone https://github.com/flutter/flutter.git -b stable flutter
cd ~
git clone https://github.com/flutter/flutter.git -b stable flutter
echo 'export PATH="$PATH:$HOME/flutter/bin"' >> ~/.bashrc
source ~/.bashrc
flutter --version
nano ~/.ssh/config
sudo apt update
sudo apt install -y nodejs npm
npx --version
npx claude-code@latest
sudo npm install -g @anthropic-ai/claude-code
# Install nvm (if you don't have it)
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash
# Then install and use a recent LTS version
nvm install --lts
nvm use --lts
sudo npm uninstall -g @anthropic-ai/claude-code
sudo npm install -g @anthropic-ai/claude-code
npx claude-code@latest
sudo npm install -g @anthropic-ai/claude-code
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.3/install.sh | bash
source ~/.bashrc   # If you're using bash (most common on Ubuntu)
# OR if you're using zsh:
# source ~/.zshrc
nvm --version
nvm install --lts
node --version   # Should now show v24.x.x
npm --version    # Should show a modern version like 10.x or higher
sudo npm install -g @anthropic-ai/claude-code
sudo npm uninstall -g @anthropic-ai/claude-code
npm install -g @anthropic-ai/claude-code
sudo npm install -g @anthropic-ai/claude-code
node --version
sudo npm install -g @anthropic-ai/claude-code
node --version
which -a node
which -a npm
echo $PATH
sudo visudo
Defaults        secure_path="/home/takawira/.nvm/versions/node/v24.13.0/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/snap/bin"
sudo visudo
sudo npm uninstall -g @anthropic-ai/claude-code
sudo npm install -g @anthropic-ai/claude-code
claude
sudo claude
