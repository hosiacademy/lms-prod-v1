// lib/src/presentation/pages/student_portal/unified_catalog_page.dart

import 'package:flutter/material.dart';

/// Unified Catalog Page — placeholder for future unified course catalog.
class UnifiedCatalogPage extends StatelessWidget {
  const UnifiedCatalogPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Unified Catalog')),
    );
  }
}
