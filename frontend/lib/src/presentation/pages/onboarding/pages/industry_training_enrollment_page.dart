// lib/src/presentation/pages/onboarding/pages/industry_training_enrollment_page.dart

import 'package:flutter/material.dart';

/// Industry Training Enrollment Page (onboarding variant) — placeholder.
class OnboardingIndustryTrainingEnrollmentPage extends StatelessWidget {
  const OnboardingIndustryTrainingEnrollmentPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Industry Training Enrollment')),
    );
  }
}
